<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:BeFitMAUI.ViewModels"
             x:Class="BeFitMAUI.Views.SessionsPage"
             Title="Treningi">
    
    <Grid RowDefinitions="Auto, *">
        <Button Text="Dodaj trening" Command="{Binding AddSessionCommand}" Margin="10" />
        
        <RefreshView Grid.Row="1" IsRefreshing="{Binding IsLoading}" Command="{Binding LoadSessionsCommand}">
            <CollectionView ItemsSource="{Binding Sessions}" SelectionMode="Single" SelectionChangedCommand="{Binding SessionSelectedCommand}" SelectionChangedCommandParameter="{Binding Source={RelativeSource Self}, Path=SelectedItem}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <SwipeView>
                            <SwipeView.RightItems>
                                <SwipeItems>
                                    <SwipeItem Text="Edytuj" BackgroundColor="Orange" Command="{Binding Source={RelativeSource AncestorType={x:Type vm:SessionsViewModel}}, Path=EditSessionCommand}" CommandParameter="{Binding .}" />
                                </SwipeItems>
                            </SwipeView.RightItems>
                            <Frame Margin="5" Padding="10">
                                <VerticalStackLayout>
                                    <Label Text="{Binding StartTime, StringFormat='{0:g}'}" FontAttributes="Bold" />
                                    <Label Text="{Binding EndTime, StringFormat='Koniec: {0:t}'}" FontSize="Small" />
                                    <Label Text="{Binding Exercises.Count, StringFormat='Ćwiczenia: {0}'}" FontSize="Small" />
                                </VerticalStackLayout>
                            </Frame>
                        </SwipeView>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
                <CollectionView.EmptyView>
                     <Label Text="Brak treningów. Rozpocznij trening!" HorizontalOptions="Center" VerticalOptions="Center" />
                </CollectionView.EmptyView>
            </CollectionView>
        </RefreshView>
    </Grid>
</ContentPage>
